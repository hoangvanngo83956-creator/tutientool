import { NextResponse } from "next/server"; import { deleteYoutubeRow, patchYoutubeTable } from "@/lib/module6-data";
export const runtime="nodejs"; type P={params:Promise<{novelId:string;cutdownId:string}>}; export async function PATCH(r:Request,{params}:P){const{novelId,cutdownId}=await params;await patchYoutubeTable("youtube_shorts_cutdowns",novelId,cutdownId,await r.json());return NextResponse.json({ok:true});} export async function DELETE(_r:Request,{params}:P){const{novelId,cutdownId}=await params;await deleteYoutubeRow("youtube_shorts_cutdowns",novelId,cutdownId);return NextResponse.json({ok:true});}
